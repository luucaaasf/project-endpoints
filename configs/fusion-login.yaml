zabbix:
  host: "fusion-login"
  groups:
    - groupid: "22"
  macros:
    '{$CERT.WEBSITE.HOSTNAME}': "https://fusion.minhabiblioteca.com.br/accounts/api/v1/sessions"
    '{$CERT.WEBSITE.IP}': "127.0.0.1"
  web_scenarios:
    - name: "Check Authenticated URL"
      delay: "5m"
      retries: "1"
      agent: "Zabbix"
      steps:
        - name: "Step Web"
          url: "https://fusion.minhabiblioteca.com.br/accounts/api/v1/sessions"
          status_codes: "201"
          timeout: "15s"
          no: 1
  triggers:
    - name: "Site https://fusion.minhabiblioteca.com.br/accounts/api/v1/sessions indisponivel"
      expression: "last(/fusion-login/web.test.fail[Check Authenticated URL],#3)=1"
      priority: 5