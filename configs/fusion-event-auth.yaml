zabbix:
  host: "fusion-event-auth"
  groups:
    - groupid: "22"
  macros:
    '{$CERT.WEBSITE.HOSTNAME}': "https://fusion.minhabiblioteca.com.br/tracker/api/v1/event"
    '{$CERT.WEBSITE.IP}': "127.0.0.1"
  web_scenarios:
    - name: "Check Authenticated URL"
      delay: "5m"
      retries: "1"
      agent: "Zabbix"
      steps:
        - name: "Step Web"
          url: "https://fusion.minhabiblioteca.com.br/tracker/api/v1/event"
          status_codes: "201"
          timeout: "15s"
          no: 1
  triggers:
    - name: "Site https://fusion.minhabiblioteca.com.br/tracker/api/v1/event indisponivel"
      expression: "last(/fusion-event-auth/web.test.fail[Check Authenticated URL],#3)=1"
      priority: 5